name: ${project.name}
version: ${project.version}
main: ${main.class}
author: ${author}

# DO NOT EDIT. This api-version is used for maximum backward compatibility.
api-version: 1.13

commands:
  spawnentity:
    aliases: [spawnmob, spawne]
  perm:
  fire:
  rank: